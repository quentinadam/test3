TODO

Reusable Github action workflow that contains a single job.
This single job contains a single step, which is to call a composite action (in this same repo).
This composite action will call a bash script. This bash script will echo the current date/time to stdout.